# Опционально: применить при Internal Server Error из-за проверки TLS бэкенда.
# После применения добавьте в lab-home.yaml в server.service.annotations:
#   traefik.ingress.kubernetes.io/service.serverstransport: vault-vault-backend-transport@kubernetescrd
# Требуется: Traefik с включённым CRD-провайдером (ServersTransport CRD).
---
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: vault-backend-transport
  namespace: vault
spec:
  insecureSkipVerify: true
